# ShellCheck Configuration for kubernetes-lab
# https://www.shellcheck.net/wiki/

# Enable all checks by default
enable=all

# Disable specific warnings that cause false positives in our codebase

# SC2034: Variable appears unused (false positives in sourced files and templates)
disable=SC2034

# SC1090: Can't follow non-constant source (dynamic sourcing is intentional)
disable=SC1090

# SC1091: Not following sourced files (external dependencies)
disable=SC1091

# SC2154: Variable referenced but not assigned (set via sourcing or environment)
# Note: We handle this carefully in our scripts
# disable=SC2154

# Shell options - enforce best practices
# Require explicit shell declaration
shell=bash

# External sources we know about
source-path=SCRIPTDIR
source-path=./shared

# File patterns to check
# By default: *.sh, *.bash, .bashrc, .bash_profile, etc.
